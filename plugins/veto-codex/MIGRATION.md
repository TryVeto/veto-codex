# Current rename: Veto Codex 2026.9.1203

The package and entry skill are `veto-codex`, previously `veto-stack`.
This is one continued method. The host identifier changes; in-place identity transfer
is not claimed. Disable the old effective plugin before enabling the replacement.
Preserve custom source, current tasks, runtime permissions, and rollback. Use the
repository-level migration guide and read-only catalog planner for the full cutover.
No model, payment, customer-message, or deployment authority changes with this rename.

The following retained notes describe prior migrations. Their source editions and
checks are not evidence that this rename has been installed.

---

# Upgrade to 2026.9.1201

Use ADOPTION.md. Keep the existing plugin identity and source; reconcile custom
changes against the supplied 1111 baseline. The outer bundle includes a patch and
its changed-file inventory. Do not automatically replace a newer installed edition.

Behavioral API change: goal routing always reports authorization_established=false.
verify_goal_readback now requires a nonempty expected task ID and objective to
verify. Missing or contradictory state no longer passes. An existing caller that
omits these inputs must obtain them from actual native context, not suppress errors.

New check_handoff.py is read-only and optional. It does not migrate, resume, accept
or delete existing work. Preserve project records outside the plugin source/cache.
No additional Python dependency, service or hook is installed.

## Historical migration notes (not the current upgrade procedure)

# Upgrade to 2026.9.1111

Base: **2026.9.1110**. This is a shareable package update, not a live installation
or source-repository audit. Stage beside the actual installed copy, compare custom
changes, keep the old package and use the supported host route at a safe boundary.
Follow [ADOPTION.md](ADOPTION.md). Do not replace the product's AGENTS.md, active
goal, settings, team, implementation or latest pause/resume instruction.

## Changed methods, unchanged authority

[Git workflow](references/git-workflow.md) is a conditional reference, not a new
active skill or mandatory repository restructure. The current repository determines
branch roles, required producers, source eligibility, integration and recovery.
The historical main/promotion model is a lead to inspect, not freshly adopted policy.

[WorkOS guidance](references/workos-environments.md) covers effective environment,
entry, membership, assurance and test evidence. The study's routing and policy
recommendations do not authorize changes. Do not confuse a report's observed
Production app name with proof of its current deployment or a credential leak.

[Delivery](references/preview-and-release.md) retains the canonical controller and
current exact-candidate approval. The new source's supervised production-qualification
proposal does not waive the existing enforced automation boundary in
[production verification](references/production-verification.md).

All **13 skill names**, **nine role guides**, acceptance contracts and both
sub-4,000-character goal templates remain. The package doctor and its regressions
change only the local marketplace validation; receipt helpers and their test
implementations remain intact. No new hooks, MCP servers, auth runner, publisher
or network helper is added. Do not install the
three archived source skills beside the adapted entries and create overlapping routes.

The native-state guard is a pure local helper plus guidance in the existing
goal/work-control routes. It classifies explicit adoption, preserves active or
paused goals when replacement is unsupported, checks same-task readback and
calculates one automation-owned `🛑 ` title prefix. The existing host reminder
delegates to the helper when available; no package hook or automatic mutation is
introduced.

[Source decisions](research/IDENTITY-GIT-PATCH.md) account for all five attachments.
Test adoption on one already-authorized relevant task; do not launch Git cleanup,
WorkOS configuration repair or production testing just because the methods exist.
