# 2026.9.1105: make the existing loop observable

Prepared 11 September 2026. This patch is an editorial and helper-code adaptation of newly supplied material. It is not a new product audit, live installation or executed agent evaluation.

## Evidence first

The supplied `veto-stack-review(1).md` reviewed Stripe Edition **2026.9.1102**. Its 121 test passes belong to that package. It found the intended limitation of an evidence-only checker: a requirement omitted before contract pinning cannot be recovered by validating that contract. It recommends temporarily freezing new skills, connecting the existing workflow and comparing behavior. It does not establish a present 1104 defect or an installed version.

The base for this patch is the actual supplied **1104** package. It already has schema-2 feedback inventory and source-audit checks, HTTP preflight and diagnosis guidance. This edition preserves that work. The new flag prevents an evidence-only contract being accidentally selected by a coverage-required invocation; semantic completeness still needs the original sources and an actual reviewer.

`Pasted markdown(20260911-220534).md` contains a sequence of prior explanations and a later correction. The final explanation reports a hardcoded file-scheme restriction before approval and no localhost rejection in that task. The linked underlying diagnostic and active host configuration were not independently inspected here. Earlier recommendations to improve auto-review are retained as historical proposals, not silently treated as the diagnosis. No documented recovery was found in that reported investigation; this is not a universal claim that recovery cannot exist.

## Source disposition

All input byte hashes and adaptation notes are recorded in [the existing source inventory](next-inputs.json). Source attachments remain outside the installable payload; no new overlapping SKILL.md is activated.

| New source | Treatment in this edition |
|---|---|
| Independent package review | [Adoption check](../ADOPTION.md), unchanged skill inventory, comparison against 1104; no claimed benchmark result. |
| Latest browser/approver transcript | First-navigation rule in existing sandbox/build entries; separate enforcing-layer diagnosis. Preserve the explicit original denial. |
| `autonomous-product-org` | Keep one accepted outcome and owner in the existing entry; do not create a second orchestrator. |
| `own-the-outcome` | Clarify ownership of investigate → act → verify → repair → handoff; preserve review/draft-only endpoints. |
| `codex-parallel-communication` | Use actual addressing/wake/return capability; distinguish send, activation, acceptance and integration; handle stale attempts. |
| `delegate-to-subagents` | Strengthen the existing work order's basis, accessible return, acceptance and unbiased question; no new assignment system. |
| `codex-plugin-engineering` | Distinguish package/load/activation/behavior proof; repair the failing layer and preserve compatibility. |
| `act-on-feedback` | Reuse original-feedback coverage, scoped correction and pause handling; no redundant feedback skill. |
| *Managing the Agentic Team* | Targeted passages at pp. 4, 16–17, 34–36, 40–49, 56, 66–68, 75–79 support outcome ownership, real handoff acceptance, proportional verification and transfer testing. Its templates and reported connected-system observations remain proposals/history, not installed systems. |

The skills were inspected for the selected operational distinctions; optional companions named inside them were not supplied as new dependencies. This is targeted consolidation, not a claim of exhaustive validation of every argument in the studies.

## Fresh external checks and their limits

Official sources checked for this patch's host-specific statements:

- [OpenAI browser guidance](https://learn.chatgpt.com/docs/browser): supports starting the development server and reviewing its local route in the supported browser. It does not establish the reported task's full denial cause or a universal prohibition of file-backed pages. The no-file-navigation rule here is a Veto preventive convention.
- [OpenAI Auto-review guidance](https://learn.chatgpt.com/docs/sandboxing/auto-review): distinguishes reviewer decisions from separate Computer Use approvals, describes compact review context and policy replacement rather than merging. It does not prove that Sebastian's effective configuration changed or that its reviewer caused a URL-layer rejection.
- [OpenAI plugin authoring](https://developers.openai.com/plugins/build/plugins): supports the local-marketplace authoring route. File validation and documented installation mechanics do not establish actual loading in the receiving host.

These current documentation checks are separate from source-derived incident reports. The workflow and `--require-feedback` mechanism are this package's adaptations, not official OpenAI features. No permission or exception is created by quoting documentation.

## Release boundary

Helpers remain local and do not run product code, use the network or change host settings. The supplied goal templates and existing product-domain references are preserved. Local tests cover helper behavior and structural compatibility. Host discovery, natural activation, actual peer coordination, original-feedback closure and comparative usefulness must be observed in the receiving environment.

The next investment is one real authorized transfer test, not another vocabulary expansion. The acceptance question is whether the team keeps and closes the full request without unnecessary founder repetition, while preserving the positive path and existing boundaries.
